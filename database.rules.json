{
  "rules": {
    "players": {
      // 참가자 목록
      ".read": true,
      // 누구나 읽기 가능 (실시간 동기화 필요)
      ".write": true
      // 개발 중: 모든 사용자 쓰기 허용
      // 프로덕션: 인증된 사용자만 쓰기 허용하려면 아래 주석 해제
      // ".write": "auth != null"
    },
    "courts": {
      // 코트 정보
      ".read": true,
      // 누구나 읽기 가능
      ".write": true
      // 개발 중: 모든 사용자 쓰기 허용
      // 프로덕션: 인증된 사용자만 쓰기 허용하려면 아래 주석 해제
      // ".write": "auth != null"
    },
    "waitingQueues": {
      // 대기열 정보
      ".read": true,
      // 누구나 읽기 가능
      ".write": true
      // 개발 중: 모든 사용자 쓰기 허용
      // 프로덕션: 인증된 사용자만 쓰기 허용하려면 아래 주석 해제
      // ".write": "auth != null"
    }
  }
}

